# RAG 与向量检索

RAG 是 Retrieval-Augmented Generation 的缩写，通常会先检索相关资料，再把资料作为上下文交给大语言模型生成答案。

向量检索的核心思想是把文本转换成 embedding 向量。语义相近的文本在向量空间中距离更近，因此可以通过余弦相似度找到和问题最相关的文档片段。

在这个项目里，我们暂时只实现检索部分：读取文档、切分 chunk、生成向量、保存索引、根据问题返回最相关的 chunk。
